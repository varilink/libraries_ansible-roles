# ------------------------------------------------------------------------------
# monitor_dashboard/tasks/main.yml
# ------------------------------------------------------------------------------

---

- ansible.builtin.import_tasks: install-service.yml

- name: Start the Grafana service
  ansible.builtin.service:
    name: grafana-server
    state: started
  become: yes
