# ------------------------------------------------------------------------------
# mail_certificates/defaults/main.yml
# ------------------------------------------------------------------------------

---
# By default use Let's Encrypt as a certification authority for all certificates
# used for IMAP and SMTP connections to the external mail server. If this is
# overridden to false, this role will fall back on self-signed certificates.
mail_uses_ca: true
