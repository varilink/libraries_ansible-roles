# Files to backup for a host of the dns_client role

Include {

  Options {

    Compression=GZIP
    Signature = MD5

  }

  # Hosts file
  File = /etc/hosts

  # Resolve configuration file
  File = /etc/resolv.conf

}
