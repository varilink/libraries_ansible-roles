---
dependencies:

  - role: wordpress_apache

  - role: site_wordpress
    vars:
      wordpress_site_server_name: "\
        {{ wordpress_site_subdomain }}.{{ domain_name }}"
      wordpress_site_path: "\
        /var/www/{{ wordpress_site_subdomain }}.{{ domain_name }}/html"
