---

dependencies:

  - role: wordpress_nginx

  - role: site_wordpress
    vars:
      wordpress_site_server_name: "\
        {{ wordpress_site.subdomain }}.{{ domain_name }}"
      wordpress_site_path: "\
        /var/www/{{ wordpress_site.subdomain }}.{{ domain_name }}/html"
