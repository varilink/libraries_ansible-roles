# ------------------------------------------------------------------------------
# database/defaults/main.yml
# ------------------------------------------------------------------------------

---

# By default do NOT expose the database service on an external network interface
# so that it will only allow connections from the same host.
database_expose_externally: false
