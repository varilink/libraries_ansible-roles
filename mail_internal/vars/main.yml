# ------------------------------------------------------------------------------
# mail_internal/vars/main.yml
# ------------------------------------------------------------------------------

---

mta_local_interfaces: '' # empty = listen on all interfaces
mta_relay_nets: "{{ office_subnet }}"
