# ------------------------------------------------------------------------------
# backup_client/handlers/main.yml
# ------------------------------------------------------------------------------

# Handlers for the backup_client role when systemd is installed.

---

- name: Restart Bacula file daemon configuration
  ansible.builtin.service:
    name: bacula-fd
    state: restarted
  listen: Bacula file daemon configuration changed
  become: yes
